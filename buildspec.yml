version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - node -v
      - npm install

  build:
    commands:
      - npm run build

artifacts:
  base-directory: out
  files:
    - '**/*'